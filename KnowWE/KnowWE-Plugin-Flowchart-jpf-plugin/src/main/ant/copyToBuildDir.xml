<?xml version="1.0" encoding="UTF-8"?>

<!--

This target copies all .js-,*.jsp-, .css-files and pics to the build target.
Can be called while running tomcat to replace above mentioned files.
So no need to build after changes in these files. Reloading the page is sufficient   

-->
<project name="project" default="copyToBildDir">

	<property name="flowchartDir" value="${basedir}/../../../../KnowWE-Plugin-Flowchart-jpf-plugin" />

	<property name="buildDir"  value="${basedir}/../../../../KnowWE/target/KnowWE-0.2-SNAPSHOT"/>
	
	<target name="copyToBildDir">

		<mkdir dir="${buildDir}/cc" />
		
		<copy todir="${buildDir}/cc" verbose="true">
			<fileset dir="${flowchartDir}/src/main/resources/webapp/cc">
				<include name="**/*.js" />
				<include name="**/*.css" />
				<include name="**/*.html" />
				<include name="**/*.gif" />
				<include name="**/*.png" />
				<include name="**/*.jpg" />
			</fileset>
		</copy>

		<copy todir="${buildDir}/" verbose="true">
			<fileset dir="${flowchartDir}/src/main/resources/webapp">
				<include name="**/*.jsp" />
			</fileset>
		</copy>


	</target>


</project>
